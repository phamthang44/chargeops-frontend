# Expo HAS CHANGED

This project is pinned to **Expo SDK 54** (`expo` exactly `54.0.2`).
Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Do NOT upgrade the `expo` package — the target device's Expo Go only supports SDK 54.
`expo` is in `package.json` → `expo.install.exclude` so `expo install`/`expo-doctor` won't bump it.
